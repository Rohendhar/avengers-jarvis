services:
  - type: web
    name: avengers-jarvis
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Tools.web_hub.app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
