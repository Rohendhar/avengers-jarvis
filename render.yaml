services:
  - type: web
    name: kinetic-pour
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 60
    envVars:
      - key: GEMINI_API_KEY
        sync: false
